import { FormEvent, useState } from "react";
import { ArrowUpRight, CheckCircle2, Handshake, Loader2, Mail, Send, Sparkles, UsersRound } from "lucide-react";
import { Link } from "wouter";
import { PageIntro, PublicLayout, usePageMeta } from "@/components/PublicLayout";
import { trpc } from "@/lib/trpc";

const paths = [
  [UsersRound, "Become a member", "Bring your perspective, lived experience and network into a community of people building what is possible.", "contact"],
  [Sparkles, "Volunteer", "Share your time, skills, local knowledge or practical support with a project that needs another pair of hands.", "contact"],
  [Handshake, "Become a partner", "Build a thoughtful pathway with EFEN, communities and the people closest to the opportunity.", "opportunity"],
  [Send, "Share an opportunity", "Open a door through a job, mentor, training, market link, resource or connection.", "opportunity"],
] as const;

type FormType = "contact" | "opportunity";

export default function Involved() {
  usePageMeta("Get involved", "Find a way to participate in EFEN's community-powered work as a member, volunteer, partner or opportunity holder.");
  const [type, setType] = useState<FormType>("opportunity");
  const [submitted, setSubmitted] = useState(false);
  const [form, setForm] = useState({ name: "", email: "", phone: "", organization: "", message: "", website: "" });
  const settings = trpc.site.settings.useQuery();
  const submit = trpc.site.submit.useMutation({ onSuccess: () => { setSubmitted(true); setForm({ name: "", email: "", phone: "", organization: "", message: "", website: "" }); } });
  const choose = (nextType: FormType) => { setType(nextType); setSubmitted(false); window.setTimeout(() => document.getElementById("involved-form")?.scrollIntoView({ behavior: "smooth", block: "start" }), 0); };
  const update = (key: keyof typeof form, value: string) => setForm((current) => ({ ...current, [key]: value }));
  const handleSubmit = (event: FormEvent) => { event.preventDefault(); submit.mutate({ type, ...form }); };
  const contact = settings.data?.contact;
  return <PublicLayout><main><PageIntro eyebrow="Get involved" title={<>There is a place for your <em>yes.</em></>} copy="Whether you bring time, expertise, resources or an opportunity, your next connection can help someone move forward." /><section className="involved-page-section"><div className="container involved-page-grid">{paths.map(([Icon, title, copy, pathType], index) => <article className="involved-page-card" key={title}><div className="involved-card-number">0{index + 1}</div><Icon size={24} /><h2>{title}</h2><p>{copy}</p><button className="text-button" onClick={() => choose(pathType)}>Choose this pathway <ArrowUpRight size={16} /></button></article>)}</div></section><section className="involved-form-section" id="involved-form"><div className="container involved-form-grid"><div className="involved-form-intro"><span className="eyebrow"><i className="eyebrow-line" /> Start a connection</span><h2>{type === "opportunity" ? "Bring an opportunity into the network." : "Tell EFEN how you would like to participate."}</h2><p>Your message will be saved securely for the EFEN administrator to review. No account is required.</p><div className="official-contact-preview">{contact?.email ? <a href={`mailto:${contact.email}`}><Mail size={16} /> {contact.email}</a> : <span><Mail size={16} /> Official email to be confirmed</span>}{contact?.phone && <span><span className="contact-separator" /> {contact.phone}</span>}</div></div><div className="involved-form-card">{submitted ? <div className="form-success"><CheckCircle2 size={34} /><h3>Thank you. Your connection is in.</h3><p>The EFEN team can now review your message from the private content studio.</p><button className="button button-dark" onClick={() => setSubmitted(false)}>Send another message</button></div> : <form onSubmit={handleSubmit}><div className="form-type-toggle"><button type="button" className={type === "contact" ? "active" : ""} onClick={() => setType("contact")}>Contact EFEN</button><button type="button" className={type === "opportunity" ? "active" : ""} onClick={() => setType("opportunity")}>Share opportunity</button></div><div className="form-fields-two"><label>Your name<input value={form.name} onChange={(e) => update("name", e.target.value)} required /></label><label>Email address<input type="email" value={form.email} onChange={(e) => update("email", e.target.value)} required /></label><label>Phone <span>(optional)</span><input value={form.phone} onChange={(e) => update("phone", e.target.value)} /></label><label>Organization <span>(optional)</span><input value={form.organization} onChange={(e) => update("organization", e.target.value)} /></label></div><label>Message<textarea rows={6} value={form.message} onChange={(e) => update("message", e.target.value)} placeholder={type === "opportunity" ? "What opportunity, resource or connection would you like to share?" : "Tell EFEN what you would like to connect about."} required /></label><label className="form-honeypot" aria-hidden="true">Website<input tabIndex={-1} autoComplete="off" value={form.website} onChange={(e) => update("website", e.target.value)} /></label><button className="button button-dark" type="submit" disabled={submit.isPending}>{submit.isPending ? <><Loader2 className="spin" size={16} /> Sending…</> : <>Send to EFEN <ArrowUpRight size={16} /></>}</button>{submit.error && <span className="form-error">We could not save this message. Please try again.</span>}</form>}</div></div></section><section className="partner-band"><div className="container partner-band-grid"><div><span className="eyebrow light"><i className="eyebrow-line" /> A useful next step</span><h2>Participation should feel safe, respectful and accessible.</h2></div><div><p>Read the questions people ask most often, or review EFEN's safeguarding commitments before you connect.</p><Link className="button button-light" href="/faqs">Read common questions <ArrowUpRight size={16} /></Link></div></div></section></main></PublicLayout>;
}
